// Placeholder: server-side user-scoped Supabase helper is intentionally disabled.
// We are using the service-role server client in `lib/supabaseServer.ts` instead.
// If you want to derive user_id from cookies later, we can re-introduce an SSR helper here.
export {};
