# Environment variables for AI integration
# Copy to .env.local and fill values

# Search provider: deepseek | tavily
# Set to 'deepseek' to use DeepSeek's native Search/Browse (requires your plan to support it).
# Set to 'tavily' to use Tavily web search.
SEARCH_PROVIDER=deepseek

# DeepSeek
DEEPSEEK_API_KEY=
# Optional: let DeepSeek attempt native browsing/search as a fallback
# Set to 1 to enable in summarize API
DEEPSEEK_SEARCH=0
your_deepseek_api_key_here

# Tavily Web Search (https://tavily.com)
# Only needed if SEARCH_PROVIDER=tavily
TAVILY_API_KEY=
your_tavily_api_key_here

# Supabase (https://supabase.com)
# Copy this file to .env.local and fill in values below.
# Client-side (exposed to browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only (do NOT expose to client)
SUPABASE_SERVICE_ROLE_KEY=
